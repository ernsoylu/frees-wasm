{ y = x^2 with an uncertain x. The propagated uncertainty is |dy/dx|*u(x) =
  2*x*u(x); reading it back with UncertaintyOf(y) forces the second solve pass. }
x = 3
UncertaintyOf(x) = 0.1
y = x^2
uy = UncertaintyOf(y)
