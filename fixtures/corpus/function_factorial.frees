{ A recursive single-output FUNCTION with IF/THEN/ELSE. 5! = 120.
  Java ProceduralFeaturesTest.functionFactorial. }
FUNCTION Factorial(n)
  IF n <= 1 THEN
    Factorial := 1
  ELSE
    Factorial := n * Factorial(n-1)
  END
END

y = Factorial(5)
