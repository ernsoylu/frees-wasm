{ A MODULE body with an internal unknown solved alongside the output.
  Java moduleSolvesInternalEquations: result = 2*3 + 1 = 7. }
MODULE Linear(m, b : y)
  y = m * x_int + b
  x_int = 3
END

CALL Linear(2, 1 : result)
