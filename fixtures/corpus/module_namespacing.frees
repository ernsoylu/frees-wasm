{ Negation and a function call inside a MODULE body must both be rewritten into
  the instance namespace. Java moduleNamespacesFunctionCallsAndNegation: out = -3. }
MODULE Shift(x : y)
  buf = -x + sin(0)
  y = buf + 1
END

CALL Shift(4 : out)
