{
  "_README": [
    "Per-fixture numeric tolerances for tests/parity.rs. Every fixture not named",
    "here is compared at the default 1e-9 relative / 1e-12 absolute.",
    "",
    "This file exists for exactly one reason, and it is not convenience.",
    "docs/decisions/0001-property-backend.md chose precomputed (P,h) tables as",
    "this build's real-fluid property source, and measured their error against",
    "native CoolProp at 1e-7..1e-4 relative. No table-backed engine can pass a",
    "1e-9 gate on a document that calls a real-fluid property function -- the",
    "gap is physics-of-the-artifact, not a bug to be found. D1 named the two",
    "honest ways out: a per-fixture tolerance, or shipping coolprop.wasm as the",
    "accuracy path. This is the first.",
    "",
    "AND THE ORACLE IS NOT COOLPROP EITHER, for one specific query shape.",
    "props/PropertyFunctions.java routes output T, Dmass or Smass at the input",
    "pair (P, Hmass) through its own PhTableRegistry -- a SaturationSplitTable",
    "built at run time on a 256/96/48 grid and gated at 1e-4 relative. Only",
    "those calls; (P,T), (P,s), (P,x) and (T,x) all fall through to the native",
    "library and the goldens for them ARE bit-exact CoolProp. Measured against",
    "direct CoolProp 8.0.0 on the sample band D1 uses: the Java runtime table is",
    "off by up to 7.9e-5 (Water Dmass, n=499) and 7.6e-5 (R134a Dmass, n=1991).",
    "The shipped 512/144/72 .phtab is 2-7x CLOSER to CoolProp than that, so on",
    "10 of the 15 intercepted water states in props_realfluid_water_states this",
    "port is nearer the physics than the golden it is graded against. A gap here",
    "is therefore |512-grid - 256-grid|, the sum of two interpolation errors --",
    "which is why the numbers below exceed the single-table error D1 measured.",
    "",
    "Rules for adding an entry:",
    "  * `relative` must be the *measured* error with a little headroom, never a",
    "    round number chosen to make a test pass. Record what was measured in",
    "    `measured`.",
    "  * `reason` must say which mechanism produces the error, so a future",
    "    session can tell a table artifact from a regression.",
    "  * An entry whose fixture passes at the default 1e-9 makes the parity test",
    "    FAIL. Dead tolerances do not accumulate here."
  ],
  "fixtures": {
    "rankine-cycle": {
      "relative": 1e-5,
      "measured": 6.406e-7,
      "reason": "Every state comes from the (P,h) split table: Enthalpy(Water,P,x), Enthalpy/Entropy(Water,P,T) by inverse bisection on the tabulated surface, Enthalpy(Water,P,s) likewise. D1 measured this document's thirteen states at <= 6.6e-5 against CoolProp 8.0.0."
    },
    "rankine-cycle-2": {
      "relative": 1e-5,
      "measured": 6.423e-7,
      "reason": "Same mechanism as rankine-cycle: an all-table steam cycle with (P,x), (P,T) and (P,s) inputs."
    },
    "refrigeration-vcr": {
      "relative": 1e-5,
      "measured": 1.528e-6,
      "reason": "R134a vapour-compression cycle. Adds P_sat(R134a, T=..) -- the (T,x) saturation-line query form -- to the (P,x)/(P,s)/(P,h) set. D1 measured (T,x) at 2.5e-6 for R134a."
    },
    "props_realfluid_water_states": {
      "relative": 2e-4,
      "measured": 7.195e-5,
      "reason": "Water through every input pair the backend serves. The worst variable is d1_rank = Density(Water, P=10 kPa, h=h_f) -- saturated-liquid density on the v_f line at f32, which the D1 error report names as this build's worst document state (6.585e-5 against CoolProp). 15 of the 44 variables here are (P,h)->T/Dmass/Smass and their goldens are the Java's own 256-grid table, not CoolProp; on 10 of those 15 this port is closer to CoolProp than the golden. Verified three-way against direct CoolProp 8.0.0."
    },
    "components_family_fluid": {
      "relative": 1e-5,
      "measured": 1.3898e-6,
      "reason": "COMPONENT network on the thermofluid bond. Every stream enthalpy comes from one Enthalpy(Water, P, T) call inside the library's Source body and is then carried unchanged along the network, so the single (P,T) inverse bisection on the tabulated surface sets the error for the whole document."
    },
    "components_family_liquid": {
      "relative": 1e-5,
      "measured": 1.2225e-6,
      "reason": "Same mechanism as components_family_fluid on the liquid bond: LiquidSource's Enthalpy(Water, P, T) is the only property call, and its (P,T) bisection error propagates to every downstream stream enthalpy."
    },
    "components_family_twophase": {
      "relative": 1e-4,
      "measured": 1.9844e-5,
      "reason": "R134a two-phase sensor. Density/Enthalpy at (P,x) on the saturation line plus Temperature(P,h) and T_sat(P) in the superheated region; the worst variable is SH = T - Tsat, a difference of two tabulated temperatures whose absolute errors do not cancel, which amplifies a 1e-7 property error into 2e-5 relative on a 31 K superheat."
    },
    "components_family_ac": {
      "relative": 1e-5,
      "measured": 3.2721e-6,
      "reason": "EXV/EXVCmd size their pressure drop from Density(R134a, P, h) at a subcooled state; the (P,h) table's density error enters the orifice equation under a square root, so it shows up doubled in the outlet pressure."
    },
    "components_bsweep_valve_characteristics": {
      "relative": 1e-5,
      "measured": 1.5837e-6,
      "reason": "Valve model$ sweep on a Water loop. The only property call is the source's Enthalpy(Water, P, T); its (P,T) inverse-bisection error is carried into every stream enthalpy the valves pass through unchanged."
    },
    "components_wave2_capillary_tube": {
      "relative": 2e-4,
      "measured": 2.9262e-5,
      "reason": "CapillaryTube with a cavitating pressure drop. dp_eff = P_in - max(P_out, P_f) subtracts the tabulated flash pressure from a boundary pressure, so a 1e-6 relative error on P_f (a (P,h)->T inversion followed by a saturation lookup) becomes 3e-5 relative on the much smaller difference."
    },
    "components_wave2_short_tube_choke": {
      "relative": 2e-4,
      "measured": 2.9262e-5,
      "reason": "Same cancellation as components_wave2_capillary_tube: the short-tube choke's dp_eff is a difference of a boundary pressure and a tabulated flash pressure, and the subtraction amplifies the table error."
    },
    "components_wave2_ejector_oilsep": {
      "relative": 1e-5,
      "measured": 5.1245e-8,
      "reason": "Ejector + oil separator on R134a. Saturated-liquid enthalpy Enthalpy(R134a, P, x=0) off the (P,x) saturation line is the only tabulated quantity and it sets the whole document's error."
    },
    "components_wave2_flash_tank": {
      "relative": 1e-5,
      "measured": 5.3369e-8,
      "reason": "Flash tank splitting a two-phase stream into its saturated liquid and vapour branches: both branch enthalpies are (P,x) saturation-line lookups from the tabulated surface."
    },
    "components_wave2_liquid_tms": {
      "relative": 1e-5,
      "measured": 1.8382e-6,
      "reason": "Two-loop liquid thermal-management network. Each LiquidSource contributes one Enthalpy(Water, P, T) inverse bisection, and both errors ride through the check valves and mixers unchanged."
    },
    "components_wave8_hydro_turbine": {
      "relative": 1e-5,
      "measured": 1.5697e-6,
      "reason": "Hydro turbine on a Water head. The inlet enthalpy is an Enthalpy(Water, P, T) bisection on the tabulated surface; the turbine's outlet enthalpy is that value minus an exactly-computed specific work, so the source error passes straight through."
    },
    "props_realfluid_r134a_states": {
      "relative": 1e-5,
      "measured": 1.879e-6,
      "reason": "R134a through the same input-pair matrix plus a superheat sweep toward the served ceiling (dh_vapor_max = 132.7 kJ/kg). Worst variable is d_pt = Density(R134a, P=1.2 MPa, T=333.15) -- a (P,T) inverse bisection on the tabulated surface, which has no Java counterpart. 24 of 42 variables are intercepted by the Java's runtime table; on 10 of those this port is closer to direct CoolProp than the golden."
    },
    "adv_cyclepath_rankine_order": {
      "relative": 1e-5,
      "measured": 3.971e-6,
      "reason": "The component-layer Rankine loop: Pump/Boiler/Turbine/Condenser instances whose bodies call Volume(Water,P,h), Entropy(Water,P,h) and Enthalpy(Water,P,s). Same (P,h) split-table mechanism as rankine-cycle -- the turbine work tu.W amplifies the isentropic-outlet enthalpy error to 4.0e-6. The component expansion itself is exact; only the property values differ."
    }
  }
}
